<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="Polish"
        android:entries="@array/languages_names"
        android:entryValues="@array/languages_names"
        android:key="source_language"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_source_language" />

    <ListPreference
        android:defaultValue="English"
        android:entries="@array/languages_names"
        android:entryValues="@array/languages_names"
        android:key="target_language"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_target_language" />

</PreferenceScreen>
